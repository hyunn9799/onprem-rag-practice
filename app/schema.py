"""검색 파이프라인 데이터 계약 (팀 공용 상수).

META_FIELDS: 청크에 실려 store → 검색 결과 → /search 응답(metadata)까지
흐르는 출처 필드. 1번 파서의 청크 스키마와 3번 챗봇의 출처 표시가 이 목록에
합의되어 있다 — 필드를 추가/삭제하려면 팀 계약을 함께 바꿔야 한다.

(청킹 로직과 무관한 순수 상수 모듈 — 팀 레포에는 이 파일만 있으면 된다.)
"""

# page   : PDF 페이지 번호 (파서가 채움 — 출처 표시의 기본)
# source : 사람이 읽는 문서 제목 (파서가 채움, 예: "효성중공업 전력기술 매거진 Vol.4 (2024)")
# 필드 추가 시(예: 기사 제목 'section') 여기 + OpenSearch 매핑 + 팀 계약을 함께 갱신.
META_FIELDS = ("page", "source")
